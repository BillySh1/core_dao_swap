import React from "react";
import { SvgProps } from "../../../components/Svg/types";

const Icon: React.FC<SvgProps> = (props) => {
  return <img src="/images/menu/info.png" width={30} height={30} />;
};

export default Icon;
